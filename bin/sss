#!/usr/bin/env scala

import java.lang.StringBuffer

var file = args[2]
var input = java.lang.StringBuffer(file)

println(parser.parse(input).toCSS())

// Still in JS

// var fs = require('fs'),
//    parser = require('../lib/parser').parser,
//    file = process.argv[2],
//    input = fs.readFileSync(file, "utf8");

// console.log(parser.parse(input).toCSS())